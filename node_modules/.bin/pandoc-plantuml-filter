#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pandoc-plantuml-filter/plantuml-filter-bin" "$@"
  ret=$?
else 
  node  "$basedir/../pandoc-plantuml-filter/plantuml-filter-bin" "$@"
  ret=$?
fi
exit $ret
